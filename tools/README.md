# `tools/`

Developer utilities. Nothing here runs in a classroom.

The lesson-graph toolchain (`lesson-compile`, `lesson-lint`, `lesson-play`) was
removed on 2026-08-18 together with the cassette it served. Bright's teacher
reads markdown from `content/library/` and decides for herself; there is nothing
left to compile. Git history keeps the tools if the reasoning is ever needed.
