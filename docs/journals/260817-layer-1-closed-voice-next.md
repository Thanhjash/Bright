# 2026-08-17 — Layer 1 closed; next is voice pipes

Owner asked: deepen the data layer, or write it down and move?

**Lock:** do not deepen Store B. Adapt at 1:1 Pre-A1 is the mark book
(`observations.mode` + `SKILL_CARD`/`PAST` + RAM `BEATS`) plus better
`keys.md`. Decision:
[layer-1-memory-is-enough.md](../decisions/layer-1-memory-is-enough.md).

**Layer 2** (`/learn` as a mouth) already met its exit. Do not polish it
into a product.

**Layer 3 cooked 2026-08-17** (same day). Same 8 tools. Stage TTS of `say`
(TeacherOS already publishes `speech.turn.*`; `speakingDriver` already
calls Piper). Then Bright ASR into `/teacher/turn`. Not Hermes voice
tools. Not AIRI. Not Gemma yet.

Gemini 3.1 Pro (`agy`) reviewed the same facts and agreed: skip Layer 2
polish, skip Store B, first voice slice is Stage not `/learn`.

Stale traps for the next agent: `architecture.md` DEGRADED/OFFLINE
cassette modes; Option B line that `classroom_say` is not live (it is
now `say`); `option-b-implementation-status.md` (2026-08-13 cassette
handoff); `runner.py` / `class_session.py` as if they were the teacher.
